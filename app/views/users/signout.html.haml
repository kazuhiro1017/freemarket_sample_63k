= render "items/hedder"  
.main
  .content2
    .signout
      .signout__container
        .signout__container__back
          = link_to "#" do
            %button.back-btn ログアウト
    = render "l-side"  
.footer
  = render 'items/footer'           